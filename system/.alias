alias zshconfig="nano ~/.zshrc"
alias bashconfig="nano ~/.bashrc"
alias themeconfig="nano ~/.p10k.zsh"

alias projects="cd ~/projects"

alias mkenv="python3 -m venv .venv"
alias activate=". .venv/bin/activate"

alias tf="terraform"
